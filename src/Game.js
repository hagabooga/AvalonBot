class Game {
  constructor() {
    // Array of players
    this.players = [];

    // Dictionary with roles as key and players as values (or null if
    // unassigned)
    this.roles = {};
  }
}

export default Game;
